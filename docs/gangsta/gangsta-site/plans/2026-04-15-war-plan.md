---
heist: gangsta-site
date: 2026-04-15
total-work-packages: 18
territories: 5
estimated-total-budget: ~150k tokens
---

# War Plan: Gangsta Documentation Site

## Territories

### Territory: Infrastructure
**Capo Domain:** Project scaffold, configuration, build pipeline, deployment
**Files:** `package.json`, `nuxt.config.ts`, `app.config.ts`, `assets/css/main.css`, `.nvmrc`, `.node-version`, `scripts/sync-skills.ts`, `.github/`, `wrangler.toml`
**Soldiers:** 1 (sequential — infrastructure must be stable before others can build)
**Budget:** ~35k tokens

### Territory: Content
**Capo Domain:** All markdown documentation pages (hand-authored sections)
**Files:** `content/**/*.md`, `content/**/*.yml`
**Soldiers:** 3 (parallel — different sections are independent)
**Budget:** ~50k tokens

### Territory: Components
**Capo Domain:** Custom Vue components and landing page
**Files:** `app/components/**/*.vue`, `content/index.md`
**Soldiers:** 2 (parallel — pipeline and hierarchy components are independent)
**Budget:** ~25k tokens

### Territory: Sync Pipeline
**Capo Domain:** GitHub sync script and Agent Skills integration
**Files:** `scripts/sync-skills.ts`, `skills/` (generated)
**Soldiers:** 1 (sequential — script must work before content pipeline is tested)
**Budget:** ~15k tokens

### Territory: Theme & Styling
**Capo Domain:** Color system, dark mode, visual polish, CSS custom properties
**Files:** `assets/css/main.css`, `app.config.ts` (styling portions), `public/` (favicon, logo)
**Soldiers:** 1 (iterative — needs visual verification)
**Budget:** ~25k tokens

---

## Work Packages

### WP-001: Project Scaffold and Dependencies
**Territory:** Infrastructure
**Contract Clause:** NFR-003, NFR-004, NFR-005
**Files:**
- Create: `package.json`
- Create: `nuxt.config.ts`
- Create: `tsconfig.json`
- Create: `.nvmrc`
- Create: `.node-version`
- Create: `.gitignore`
**Acceptance Criteria:**
1. `npm install` completes without errors
2. `package.json` has pinned versions for docus, nuxt, better-sqlite3
3. `.nvmrc` and `.node-version` contain "20"
4. `engines` field in package.json requires node >= 20
**Verification:** `npm install && node -e "require('./package.json').engines.node"`
**Budget:** ~5k tokens
**Dependencies:** None

### WP-002: Docus Configuration
**Territory:** Infrastructure
**Contract Clause:** FR-012, NFR-001, NFR-008, AD-006, AD-007
**Files:**
- Create: `app.config.ts`
**Acceptance Criteria:**
1. Docus title is "Gangsta"
2. Color mode is forced to dark (no toggle)
3. GitHub integration points to kucherenko/gangsta
4. Navigation structure is defined
5. Social links include GitHub
6. Site URL is configured for SEO/sitemap
**Verification:** `npm run dev` loads site with correct config
**Budget:** ~5k tokens
**Dependencies:** WP-001

### WP-003: Nuxt Configuration and Cloudflare Setup
**Territory:** Infrastructure
**Contract Clause:** NFR-003, AD-005
**Files:**
- Create: `nuxt.config.ts`
- Create: `wrangler.toml` (Cloudflare Pages config)
**Acceptance Criteria:**
1. `nuxt.config.ts` extends docus layer
2. Nitro preset is 'cloudflare-pages'
3. Content highlight config includes typescript, javascript, bash, yaml, json, markdown
4. CSS references `~/assets/css/main.css`
5. `wrangler.toml` has correct build output dir
**Verification:** `npm run build` produces `.output/public/` with static files
**Budget:** ~5k tokens
**Dependencies:** WP-001

### WP-004: Gold/Amber Theme and Dark Mode CSS
**Territory:** Theme & Styling
**Contract Clause:** NFR-001, NFR-002, NFR-006 (contrast)
**Files:**
- Create: `assets/css/main.css`
**Acceptance Criteria:**
1. `@import "tailwindcss"` present
2. 11-shade gold palette defined in oklch via `@theme`
3. Surface/near-black tones defined for backgrounds
4. Docus `--ui-*` CSS custom properties remapped to gold palette
5. Interactive text shades achieve 4.5:1 contrast ratio against background (WCAG AA)
6. Code block colors remapped
**Verification:** `npm run dev` renders site with gold accents on dark background; no green default colors visible
**Budget:** ~8k tokens
**Dependencies:** WP-002

### WP-005: Public Assets (Logo, Favicon, OG Image)
**Territory:** Theme & Styling
**Contract Clause:** NFR-008
**Files:**
- Create: `public/favicon.ico`
- Create: `public/logo.svg`
- Create: `public/og-image.png`
**Acceptance Criteria:**
1. Favicon renders in browser tab (gold "G" on dark square)
2. Logo SVG renders in header (text-based "GANGSTA" or icon suitable for Docus header)
3. OG image exists (1200x630 dark card with title)
**Verification:** `npm run dev` shows favicon and logo in header
**Budget:** ~5k tokens
**Dependencies:** WP-002

### WP-006: Landing Page
**Territory:** Components
**Contract Clause:** FR-001
**Files:**
- Create: `content/index.md`
**Acceptance Criteria:**
1. Hero section with title, description, and CTA buttons (Get Started + GitHub)
2. Feature section with 6 features (Spec Before Code, 20 Skills, 1% Rule, Multi-Agent, 5 Platforms, Institutional Memory)
3. Pipeline section with `::pipeline-flow` component embed
4. Hierarchy section with `::borgata-hierarchy` component embed
5. Phase Gate callout section linking to Core Concepts
6. `navigation: false` in frontmatter (landing page not in sidebar)
**Verification:** `npm run dev` renders landing page at `/` with all sections
**Budget:** ~8k tokens
**Dependencies:** WP-002, WP-004

### WP-007: Pipeline Flow SVG Component
**Territory:** Components
**Contract Clause:** FR-010, NFR-006
**Files:**
- Create: `app/components/content/PipelineFlow.vue`
**Acceptance Criteria:**
1. Renders 6-phase horizontal flow on desktop (Reconnaissance → The Grilling → The Sit-Down → Resource Dev → The Hit → Laundering)
2. Vertically stacks on mobile viewport
3. Each phase shows: number, name, skill invocation, gate artifact
4. Chevron arrows between phases
5. Gold accent colors on dark background
6. `aria-label="The Heist Pipeline"` and `role="img"`
7. Pure static SVG — no `<ClientOnly>`, no `onMounted`, no `ref()`
**Verification:** Component renders in dev server on both mobile and desktop viewports
**Budget:** ~6k tokens
**Dependencies:** WP-004

### WP-008: Borgata Hierarchy SVG Component
**Territory:** Components
**Contract Clause:** FR-011, NFR-006
**Files:**
- Create: `app/components/content/HierarchyTree.vue`
**Acceptance Criteria:**
1. Renders org-tree: Don → Consigliere + Underboss → Capo → Soldier/Associate, with The Ledger as side element
2. Don node has gold border, Consigliere has dashed gold border
3. Role names and skill invocations displayed
4. Dark background, gold/amber accents
5. `aria-label="The Borgata Hierarchy"` and `role="img"`
6. Pure static CSS/SVG — no `<ClientOnly>`, no `onMounted`, no `ref()`
7. Responsive layout
**Verification:** Component renders in dev server
**Budget:** ~6k tokens
**Dependencies:** WP-004

### WP-009: Phase Gate Callout Component
**Territory:** Components
**Contract Clause:** FR-004
**Files:**
- Create: `app/components/content/PhaseGate.vue`
**Acceptance Criteria:**
1. Renders a styled callout with lock icon and message "This phase requires explicit Don approval before proceeding. Never auto-advance."
2. Uses gold accent color
3. Can be embedded in any markdown via `::phase-gate`
**Verification:** `::phase-gate` renders in a markdown page
**Budget:** ~2k tokens
**Dependencies:** WP-004

### WP-010: Content — Getting Started Section
**Territory:** Content
**Contract Clause:** FR-002, AC-012
**Files:**
- Create: `content/1.getting-started/.navigation.yml`
- Create: `content/1.getting-started/index.md` — Overview of Gangsta
- Create: `content/1.getting-started/installation.md` — Platform picker + install instructions
- Create: `content/1.getting-started/quickstart.md` — First Heist in 5 minutes
- Create: `content/1.getting-started/the-1-percent-rule.md` — 1% Rule explained
**Acceptance Criteria:**
1. Sidebar shows "Getting Started" section with 4 pages
2. Installation page covers all 5 platforms (Claude Code, OpenCode, Codex, Gemini CLI, Cursor)
3. Quickstart walks through a complete mini-Heist
4. 1% Rule page explains the invocation requirement
**Verification:** `npm run dev` renders all 4 pages with correct sidebar navigation
**Budget:** ~12k tokens
**Dependencies:** WP-002

### WP-011: Content — Core Concepts Section
**Territory:** Content
**Contract Clause:** FR-003
**Files:**
- Create: `content/2.core-concepts/.navigation.yml`
- Create: `content/2.core-concepts/index.md` — Concepts map
- Create: `content/2.core-concepts/the-borgata.md` — Hierarchy (embeds HierarchyTree)
- Create: `content/2.core-concepts/the-heist.md` — Pipeline overview (embeds PipelineFlow)
- Create: `content/2.core-concepts/omerta.md` — Five Laws
- Create: `content/2.core-concepts/intent-routing.md` — Intent routing table
- Create: `content/2.core-concepts/the-ledger.md` — Institutional memory
- Create: `content/2.core-concepts/spec-driven-dev.md` — Why SDD
**Acceptance Criteria:**
1. Sidebar shows "Core Concepts" with 7 pages
2. Hierarchy page embeds `::borgata-hierarchy` component
3. Pipeline page embeds `::pipeline-flow` component
4. Omerta page lists all 5 laws with descriptions
5. Intent routing page shows the intent routing table
**Verification:** All pages render with correct navigation
**Budget:** ~12k tokens
**Dependencies:** WP-002, WP-007, WP-008

### WP-012: Content — Heist Pipeline Deep-Dive
**Territory:** Content
**Contract Clause:** FR-004, AC-013
**Files:**
- Create: `content/3.heist-pipeline/.navigation.yml`
- Create: `content/3.heist-pipeline/index.md` — Pipeline overview with PipelineFlow embed
- Create: `content/3.heist-pipeline/reconnaissance.md`
- Create: `content/3.heist-pipeline/the-grilling.md`
- Create: `content/3.heist-pipeline/the-sit-down.md`
- Create: `content/3.heist-pipeline/resource-development.md`
- Create: `content/3.heist-pipeline/the-hit.md`
- Create: `content/3.heist-pipeline/laundering.md`
**Acceptance Criteria:**
1. Sidebar shows "Heist Pipeline" with 7 pages (overview + 6 phases)
2. Each phase page includes: purpose, process, checklist, outputs/artifacts, gate conditions
3. Each phase page ends with `::phase-gate` callout
4. Overview page embeds `::pipeline-flow` component
**Verification:** All 7 pages render with Phase Gate callout
**Budget:** ~10k tokens
**Dependencies:** WP-002, WP-009

### WP-013: Content — Advanced Section
**Territory:** Content
**Contract Clause:** FR-006
**Files:**
- Create: `content/5.advanced/.navigation.yml`
- Create: `content/5.advanced/index.md` — Advanced topics overview
- Create: `content/5.advanced/custom-skills.md` — Writing your own skills
- Create: `content/5.advanced/multi-agent-patterns.md` — Orchestration with Underboss/Capo
- Create: `content/5.advanced/checkpoint-recovery.md` — Resuming interrupted Heists
- Create: `content/5.advanced/contributing.md` — Contributing to Gangsta
**Acceptance Criteria:**
1. Sidebar shows "Advanced" with 5 pages
2. Custom skills page explains SKILL.md format and directory structure
3. Multi-agent page explains Underboss/Capo orchestration
4. Checkpoint page explains resuming from docs/gangsta/
**Verification:** All pages render with correct sidebar
**Budget:** ~8k tokens
**Dependencies:** WP-002

### WP-014: GitHub Sync Script
**Territory:** Sync Pipeline
**Contract Clause:** FR-007, FR-008
**Files:**
- Create: `scripts/sync-skills.ts`
**Acceptance Criteria:**
1. Script pulls from `kucherenko/gangsta` (default) with configurable owner/repo/path via env vars
2. Copies raw files to `skills/` directory (maintaining subdirectory structure)
3. Generates Docus content pages in `content/4.skills/` with injected frontmatter (title, description, navigation.order, navigation.icon)
4. Generates `content/4.skills/.navigation.yml`
5. Requires `GITHUB_TOKEN` env var — fails with clear error if unset
6. Handles subdirectories (e.g., `the-grilling/proposer-prompt.md`)
7. Fails loudly on API errors with descriptive message
**Verification:** `npm run sync` populates `skills/` and `content/4.skills/` directories
**Budget:** ~8k tokens
**Dependencies:** WP-001

### WP-015: Skills Content Navigation Configuration
**Territory:** Sync Pipeline
**Contract Clause:** FR-005
**Files:**
- Create: `content/4.skills/.navigation.yml` (generated by sync script)
- Create: `content/4.skills/index.md` — Skills reference index page
**Acceptance Criteria:**
1. Skills index page shows a catalog/grid of all skills with type badges
2. Skills are organized into sub-categories in sidebar (Hierarchy Roles, Heist Phases, Dev Skills)
3. Each skill page shows title, invocation, description, process, outputs
**Verification:** Skills section renders in sidebar with proper categorization
**Budget:** ~4k tokens
**Dependencies:** WP-002, WP-014

### WP-016: Build Pipeline Integration
**Territory:** Infrastructure
**Contract Clause:** NFR-003, NFR-005, FR-008
**Files:**
- Modify: `package.json` (scripts)
**Acceptance Criteria:**
1. `npm run sync` executes `tsx scripts/sync-skills.ts`
2. `npm run dev` starts dev server
3. `npm run build` runs `npm run sync && nuxt generate`
4. `npm run preview` serves static output locally
5. All dependency versions are pinned (no "latest")
**Verification:** `npm run build` produces `.output/public/` directory with static HTML files
**Budget:** ~3k tokens
**Dependencies:** WP-001, WP-003, WP-014

### WP-017: Cloudflare Pages Configuration
**Territory:** Infrastructure
**Contract Clause:** AD-005, FR-009
**Files:**
- Create: `wrangler.toml`
- Create: `.github/workflows/deploy.yml` (optional CI/CD reference)
**Acceptance Criteria:**
1. `wrangler.toml` configured for Cloudflare Pages static deployment
2. Build command: `npm run build`
3. Output directory: `.output/public`
4. Node.js version set to 20
5. Environment variable `GITHUB_TOKEN` documented
**Verification:** Configuration files exist with correct values
**Budget:** ~3k tokens
**Dependencies:** WP-003, WP-016

### WP-018: Static SVG Placeholder Assets
**Territory:** Theme & Styling
**Contract Clause:** FR-001 (landing page needs assets)
**Files:**
- Create: `public/favicon.svg` (SVG favicon — gold G on dark)
- Create: `public/favicon.ico` (can be placeholder)
- Create: `public/og-image.png` (1200x630 placeholder)
**Acceptance Criteria:**
1. Docus serves favicon correctly
2. OG image renders in social previews
3. Logo SVG renders in header
**Verification:** `npm run dev` shows favicon and header logo
**Budget:** ~3k tokens
**Dependencies:** WP-002

---

## Execution Order

### Group 1 — Foundation (Sequential)
These must execute in order; each depends on the previous:
1. **WP-001**: Project Scaffold and Dependencies
2. **WP-002**: Docus Configuration (depends WP-001)
3. **WP-003**: Nuxt Configuration and Cloudflare Setup (depends WP-001)

### Group 2 — Theme & Assets (Parallel, after Group 1)
Can run in parallel after WP-002:
4. **WP-004**: Gold/Amber Theme CSS (depends WP-002)
5. **WP-005**: Public Assets (depends WP-002)
6. **WP-009**: Phase Gate Component (depends WP-004)

### Group 3 — Sync Pipeline (Parallel, after Group 1)
Can run in parallel with Group 2:
7. **WP-014**: GitHub Sync Script (depends WP-001)
8. **WP-015**: Skills Content Navigation (depends WP-002, WP-014)

### Group 4 — Components (Parallel, after WP-004)
9. **WP-007**: Pipeline Flow SVG (depends WP-004)
10. **WP-008**: Borgata Hierarchy SVG (depends WP-004)

### Group 5 — Content Pages (Parallel, after WP-002)
11. **WP-010**: Getting Started Section (depends WP-002)
12. **WP-011**: Core Concepts Section (depends WP-002, WP-007, WP-008)
13. **WP-012**: Pipeline Deep-Dive (depends WP-002, WP-009)
14. **WP-013**: Advanced Section (depends WP-002)

### Group 6 — Integration (Sequential, after all above)
15. **WP-006**: Landing Page (depends WP-002, WP-004, WP-007, WP-008)
16. **WP-016**: Build Pipeline Integration (depends WP-001, WP-003, WP-014)
17. **WP-017**: Cloudflare Pages Configuration (depends WP-003, WP-016)
18. **WP-018**: Static SVG Placeholder Assets (depends WP-002)

---

## Baseline Verification
- Tests: N/A (no existing tests — this is a greenfield project)
- Dependencies: Install and verify Docus v5.9.0+ with Nuxt 4
- Branch: To be created `heist/gangsta-site` from initial commit

## Critical Path
```
WP-001 → WP-002 → WP-004 → [WP-007, WP-008] → WP-006
                  → WP-014 → WP-015
                  → [WP-010, WP-011, WP-012, WP-013]
         → WP-003 → WP-016 → WP-017
```

Total estimated time: ~8-12 hours for a single developer, ~4-6 hours with parallel execution